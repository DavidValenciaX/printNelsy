/**
 * Crea una nueva página en el canvas
 * @param {fabric.Canvas} canvas - Instancia del canvas
 */
export function createNewPage(canvas) {
    console.log('Creando una nueva página...');
    // Lógica para crear una nueva página (se implementará después)
  }